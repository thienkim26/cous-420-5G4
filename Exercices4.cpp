#include <iostream>
using namespace std;

int main( ){
    int nombre, initial, inverser = 0;

    cout << "Entrée utilisateur : Nombre à vérifier = " << nombre << endl;

    inital = nombre;


    if(initial == inverser) {
        cout << " Sortie " << initial << " est un nombre palindrome. " << endl;
    } else {
        cout << " Sortie " << initial << " n'est pas un nombre palindrome. " << endl;
    }
    
   return 0;
}